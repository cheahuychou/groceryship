{{!-- Author: Czarina Lao --}}
<div id="set-pickup-modal" class="modal fade" role="dialog">
  <div class="modal-messages"></div>
  <div class="modal-dialog modal-dialog-wide">
    <div class="modal-content">
      <div class="modal-header">
        <button type="button" class="close" data-dismiss="modal">&times;</button>
        <h3 class="modal-title">Set Pickup Time</h3>
      </div>
      <div class="modal-body">
        <table class="table table-hover">
          <thead>
            <tr>
              {{!-- given --}}
              <th class='requester'>Requester</th>
              <th class='item'>Item</th>
              <th class='pickup-point'>Pickup Point</th>
              {{!-- to be filled out --}}
              <th class='pickup-time'>Pickup Time</th>
              <th class='price'>Price</th>
            </tr>
          </thead>
          <tbody>
            {{!-- use js to populate with selected requests --}}
          </tbody>
        </table>
        {{!-- add button event handler --}}
        <div class='row'>
          <div class='col-xs-offset-10 col-xs-2'>
            <button id='deliver-confirm-button' class='btn btn-primary'>Confirm</button>
          </div>
        </div>
      </div>     
    </div>
  </div>
</div>
